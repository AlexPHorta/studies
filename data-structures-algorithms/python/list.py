# List
#
# Subsumes FIFO Queue, Stack and Deque interfaces.# List
#
# Includes the following operations:
# - size() - return the length of the list
# - get(i) - return the value x[i]
# - set(i, x) - set the value of x[i] equal to x
# - add(i, x) - add x at position i, displacing x[i],...,x[n-1]
# - remove(i) - remove the value x[i], displacing x[i+1],...,x[n-1]
###############################################################################
